@model IEnumerable<AssessoriaWeb.Models.Pessoa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pes_nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pes_cpf)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pes_sexo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pes_datanascimento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pes_telefone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pes_email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pes_login)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.pes_nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pes_cpf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pes_sexo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pes_datanascimento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pes_telefone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pes_email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pes_login)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.pes_id }) |
                @Html.ActionLink("Details", "Details", new { id = item.pes_id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.pes_id })
            </td>
        </tr>
    }

</table>
