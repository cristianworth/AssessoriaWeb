@model IEnumerable<AssessoriaWeb.Models.Nutricionista>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pessoa.pes_nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nut_crn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nut_cnpj)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pessoa.pes_nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nut_crn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nut_cnpj)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.nut_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.nut_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.nut_id })
        </td>
    </tr>
}

</table>
