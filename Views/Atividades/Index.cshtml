@model IEnumerable<AssessoriaWeb.Models.Atividade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ati_descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.atl_observacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ati_descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.atl_observacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ati_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.ati_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ati_id })
        </td>
    </tr>
}

</table>
